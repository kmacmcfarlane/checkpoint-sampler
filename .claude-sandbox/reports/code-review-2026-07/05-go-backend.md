# Aspect 5: Go backend quality

Build clean, make lint / go vet clean, model package has no serialization tags (compliant).

- **[high]** [stale path layout] `backend/internal/service/sample_job.go:384` — missingOnly job creation checks `{sampleDir}/{study}/{checkpoint}/{file}` but executor writes to fileformat.StudyOutputDir = `{sanitizedRun}/{study}[/{baseModel}]` (job_executor_conn.go:583). FileExists never matches → missing-only silently regenerates everything. Fix: use fileformat.StudyOutputDir in filter.
- **[high]** [stale path layout] `backend/internal/store/filesystem.go:317-318` (via service/sample_job.go:966) — RemoveJobSampleDir deletes `{sampleDir}/{study}/{checkpoint}`, missing run/base-model levels. Delete(deleteData=true) removes nothing; API returns success, files remain. Fix: route through StudyOutputDir.
- **[high]** [orchestration gap] `backend/internal/service/sample_job.go:527-599` — service Start (POST start) sets pending→running in DB but never calls executor.RequestResume(id); executor poll only auto-starts pending (job_executor_lifecycle.go:322-335). If API write wins the 1s race, job sits running forever until restart. Fix: call RequestResume after status write (as Resume does, line 911).
- **[medium]** [S-161 regression] `sample_job.go:1218` — GenerateOutputFilenameWithDims keys LoRA strength params on item.LoraModelPath != ""; since S-161 items created with empty path → creation-time filenames lack strength_model/clip while executor writes with them. Missing-only can never match LoRA samples even after dir fix. Fix: key on job.BaseModel != "" or strength fields.
- **[medium]** [S-161 consistency] `job_executor_lifecycle.go:543-554` — SubmitPrompt connection error marks disconnected but still failItem() → transient outage = failed item + completed_with_errors. Inconsistent with resolution branch (470-485) which leaves pending. Fix: mirror resolution path in isConnectionError branch.
- **[medium]** [lock discipline] `job_executor_lifecycle.go:294-351`, `job_executor_conn.go:287-302` — store I/O under e.mu; WS read loop blocks on e.mu → poll ticks stall progress/completion delivery; SetMaxOpenConns(1) extends. Fix: snapshot under lock, query unlocked, compare-and-act.
- **[low]** [lifecycle] `job_executor_lifecycle.go:52-76` — double Stop panics on close(shutdownCh); `store/comfyui_ws.go:150-198` Connect-after-Close never recreates stopCh → can't reconnect. Safe under current wiring; footguns. Fix: sync.Once / recreate stopCh.
- **[low]** [error handling] `job_executor_progress.go:29-31` — failItem re-reads e.activeJobID which may be cleared → failure silently drops, item stays running → orphan-reset can infinite-retry deterministic failures. Fix: pass jobID through from event site.
- **[low]** [error handling] `api/sample_jobs.go:173,190,210` — GetItemCounts errors swallowed; zero counts on DB failure. Fix: log warn.
- **[low]** [duplication] checkpoint progress aggregation implemented 3x: service/sample_job.go:1062-1154, job_executor_progress.go:371-459, store/sample_job.go:617-705 — already drifted once (B-162 class). Consolidate.

**Verdict:** Concurrency core carefully engineered. Real problems: stale filesystem-layout assumptions from run-name/base-model restructuring (missing-only + delete-data silently broken) plus two orchestration gaps undercutting S-161 offline queuing.
