services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.58.2-noble
    working_dir: /app
    volumes:
      - ./frontend:/app
      - playwright_node_modules:/app/node_modules
    environment:
      - PLAYWRIGHT_BASE_URL=http://frontend:3000
    networks:
      - default

volumes:
  playwright_node_modules:

networks:
  default:
    external: true
    name: checkpoint-sampler-dev_default
